# 简单递归函数

``` c++
#include<bits/stdc++.h>
using namespace std;
int feb(int x)// 递归函数：在函数里调用自己
{
    if(x==1) return 1;
    if(x==2) return 1;
    int s=feb(x-1)+feb(x-2);
    return s;
}
int main()
{
    int x;
    cin>>x;
    cout<<feb(x)<<endl;
    return 0;
}
```